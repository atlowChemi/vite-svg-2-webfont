/** Absolute path to the default CSS template (`css.hbs`). */
declare const css: string;
/** Absolute path to the default HTML preview template (`html.hbs`). */
declare const html: string;
/** Absolute path to the default SCSS template (`scss.hbs`). */
declare const scss: string;

export { css, html, scss };
